# Jev / Evidence atlas

User rejected both emoji tables and ASCII receipts. Replace the report identity; preserve exact evidence and comment-only behavior. Native GitHub Markdown is the compact entry, while a standalone HTML artifact carries exploration.

Seven grounded directions considered: instrument panel, code-review notebook, mutation-testing scoreboard, engineering drawing, courtroom evidence register, flight recorder, scientific experiment plate. Assigned direction: scientific experiment plate. Experiment comparison and source traceability fit maintainer reading better than decorative dashboard widgets.

World: warm white paper, ink typography, cobalt observed safe-state marks, vermilion broken-state marks. Use a large editorial question, paired probability traces on a shared axis, narrow evidence labels, and a dark source-code inspection surface. Colors never carry meaning alone. No emoji, fake terminal chrome, progress rings, gauges, gradient text or chart decoration.

Challenger disciplines considered: dense Japanese utility layouts (declined: keep compact comparisons, avoid tiny type); stitched sampler (declined: keep sequential evidence, avoid decorative cloth); amber jackfield (competitive: precise links, but source-code reading wins on paper); daylight section (declined: make transitions legible, no scene illustration). No new user choice inferred; autonomous direction selection follows explicit delegation.

Read mode. Signature interaction: select a fixture to inspect exact source and outcomes; change a hypothetical approval threshold to see which synthetic cases would pass the probability threshold. This simulation never changes deployed policy and does not establish calibration.

Responsive: one-column reading and horizontally bounded plots on narrow screens. Keyboard-operable case buttons, explicit focus, reduced motion, text equivalents for every plotted value. Evidence and source links remain available without JavaScript.
