# X-ray and experiment atlas validation — 2026-09-16

## Live observations

[PR #2 review](https://github.com/thiago-ss/jev-review/pull/2#pullrequestreview-5226786834) now displays correlated perspective distributions, per-file hypotheses, CI evidence and the experiment poster. [App workflow](https://github.com/thiago-ss/jev-review/actions/runs/35138189010) succeeded on `fa2fb34`: all three additional requests returned `jev-1.13.0`. Correctness selected approve (0.46), security approve (0.64), verification hold (0.38). These selected probabilities are not three calibrated independent votes. [Full pilot record](xray-pilot.json).

The existing review marker correctly prevented a duplicate comment. Its body was then updated through the installed App using the captured workflow evidence; baseline details were collapsed and the graph endpoint clarified as human comparison. No approval or reviewer request occurred.

The separate [synthetic lab](stress-lab.json) made exactly ten calls, with ten responses and no transport failures. All six expected-unsafe variants were held. The safe integer multiplication case was also held, and three other safe cases selected defect classes despite approving. Six of ten records matched both the expected disposition and failure class. Labels are authored, limited synthetic expectations, not independent production labels. Question version and exact states are retained. The source hash records the experimental module at capture time, before subsequent generic-exception sanitization; fixture/question content did not change afterward.

## Validation

- Luna implementers supplied investigation and stress-lab modules/tests. Orchestrator reviewed trust boundaries, removed answer cues from inputs, required valid hunk counts and contextual code, integrated the workflow and authored report rendering.
- [Implementation CI](https://github.com/thiago-ss/jev-review/actions/runs/35138178322): 92 tests pass on Python 3.9 and 3.12; mypy passes for 13 source files.
- Browser verified real GitHub Mermaid rendering and the loaded 1800×1440 poster, plus collapsed baseline and evidence details.
- Browser tested atlas case selection and a 5% hypothetical threshold: five cases meet it, including one expected-unsafe case. This simulator omits deployed policy gates and cannot change them.
- Desktop and 390px mobile report inspection completed. Source text wraps within the inspector; wide measurement plot scrolls in its own container. Script-injection tests confirm untrusted case text remains inert; missing responses are not plotted as zero.
- Secret scan passed. HTML contains no external scripts, credentials or analytics. Reports remain within the private repository and local workspace.

## Limits

Three prompts still query one correlated model. Per-file labels are hypotheses, not validated line-level findings. No PR code is executed by this bot. No independently calibrated approval readiness is established. The static lab report is a captured experiment; new PR investigations are generated by the enabled comment-only workflow. Rebuild the atlas explicitly after a new experiment.
